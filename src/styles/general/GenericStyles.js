const input = {
    marginBottom: 10,
    marginTop: 10,
    backgroundColor: '#fff2ce',
    height: 50
};

const titleText = {
    alignSelf: 'center',
    fontSize: 20,
    color: '#000000',
    margin: 20
}

const onlyBackgroundColor = {
    backgroundColor: '#ffcf32'
};

const signUpScreen = {
    flex:1,
    backgroundColor: '#f6b26b',
    paddingLeft: 15,
    paddingRight: 15
}

const title = {
    textAlign: 'center',
    fontSize: 25,
    marginBottom: 35
};

const button = {
    backgroundColor: '#3EB489',
    paddingTop: 15,
    paddingBottom:15,
    width: 100,
    alignSelf: 'center',
    marginTop: 30,
    marginBottom:30,
    borderRadius: 10
};

const secondaryButtonStyle = {
    backgroundColor:'#246EE9',
    paddingTop: 15,
    paddingBottom:15,
    width: 150,
    alignSelf: 'center',
    marginTop: 30,
    marginBottom:30,
    borderRadius: 10
};

const buttonText = {
    textAlign: 'center',
    fontSize: 13,
    color: 'white'
};

const secondaryButtonText = {
    textAlign: 'center',
    fontSize: 15,
    color: 'white'
};

const image = {
    height:  200,
    width: 200,
    resizeMode: 'contain'
};

const signUpImage = {
    height:  400,
    width: 300,
    resizeMode: 'contain'
};

const imageContainer = {
    padding: 10
};

export {
    input, signUpScreen, title, button,
    secondaryButtonStyle, buttonText, secondaryButtonText, image,
    imageContainer, signUpImage, titleText, onlyBackgroundColor
};
